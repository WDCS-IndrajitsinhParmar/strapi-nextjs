type Status = 'success' | 'failed'

function useAuth() {



    return {
        authenticated: ''//token && signedIn,
        // signIn,
        // signUp,
        // signOut,
    }
}

export default useAuth
